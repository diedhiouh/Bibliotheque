{% extends 'base.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1 class="jumbotron">Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ domaine.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">Liste des categories</a>

    <a href="{{ path('categorie_edit', {'id': domaine.id}) }}">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
